[Unit]
Description=Reboot if a kernel update is pending
ConditionPathExists=/var/run/reboot-required
After=network.target

[Service]
Type=oneshot
ExecStart=/sbin/shutdown -r +1 "Rebooting: kernel update pending"
